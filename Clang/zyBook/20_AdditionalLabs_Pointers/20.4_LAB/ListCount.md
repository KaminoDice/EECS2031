# 20.4 LAB: List count

Given the IntNode struct, define the GetCount() function that returns the number of items in the list not including the head node.

Ex: If the list contains:

```
head -> 14 -> 19 -> 4
```

GetCount(headNode) returns 3.

Ex: If the list contains:

```
head ->
```

GetCount(headNode) returns 0.