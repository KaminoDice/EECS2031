Consider the following commands performed on the lines from file emp.txt.c

$ sed -n ‘1,2p’ emp.txt.c

$ sed  ‘3,$d’ emp.txt.c

Which of the statement (given as options below) about the output of both commands is correcto?

What is the output of the following piece of code?

    code=6

    val=20

    echo $((  code  ?  val-1  :   val+1  ))

For the expression:  [  “$a” -gt  5  ] || [  “$b”  -lt   10  ] || [   “$c”   -gt    20 ]

What values of a and b do not result in short circuit evaluation, such that all operands are evaluated? Note: assume that the code will execute properly and there are no syntax or runtime issues. 

Question 3 Answer
a.

 a = 6, b = 10
b.

None of the answers
c.

a = 4, b = 9
d.

a = 3, b = 11
